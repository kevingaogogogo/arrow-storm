# 箭雨 Arrow Storm

竖屏 540x960 虚拟分辨率 Canvas 2D 飞行射箭 Roguelite。所有 CSS 和 JavaScript 都内联在 `index.html`，不依赖外部资源。

## 运行

直接用浏览器打开 `index.html` 即可游玩。

## 操作

- 键盘：`←` / `→` 或 `A` / `D` 左右移动
- 触摸：按住左/右半屏移动，或拖拽让角色跟随手指横向移动
- 右上角：静音开关

每击杀 12 个敌人会暂停并二选一升级；每 5 波出现 Boss。最高分会保存在 `localStorage`。
